{
  "name": "@cf-ai/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ../../infra/worker.wrangler.toml",
    "deploy": "wrangler deploy --config ../../infra/worker.wrangler.toml",
    "test": "vitest --run" 
  },
  "dependencies": {
    "itty-router": "5.0.18",
    "zod": "3.23.8"
  }
}

